# System Architecture - Soofi Attars

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ index.html   │  │billing.html  │  │inventory.html│          │
│  │ (Dashboard)  │  │ (Billing)    │  │ (Inventory)  │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                  │                   │
│         └─────────────────┴──────────────────┘                   │
│                           │                                      │
│                    ┌──────▼──────┐                              │
│                    │   app.js    │                              │
│                    │ (Frontend   │                              │
│                    │  Logic)     │                              │
│                    └──────┬──────┘                              │
│                           │                                      │
│                    ┌──────▼──────────┐                          │
│                    │api-integration.js│                         │
│                    │  (API Client)    │                         │
│                    └──────┬───────────┘                         │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                            │ HTTPS/REST API
                            │
┌───────────────────────────▼──────────────────────────────────────┐
│                    VERCEL (Serverless)                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    api/index.js                          │   │
│  │              (Express.js Application)                    │   │
│  └────┬─────────────────────┬──────────────────────┬────────┘   │
│       │                     │                      │             │
│  ┌────▼────┐          ┌─────▼─────┐         ┌─────▼─────┐      │
│  │ Auth    │          │ Inventory │         │  Sales    │      │
│  │ Routes  │          │  Routes   │         │  Routes   │      │
│  └────┬────┘          └─────┬─────┘         └─────┬─────┘      │
│       │                     │                      │             │
│       └─────────────────────┴──────────────────────┘             │
│                             │                                    │
│                      ┌──────▼──────┐                            │
│                      │ Middleware  │                            │
│                      │   (Auth)    │                            │
│                      └──────┬──────┘                            │
│                             │                                    │
│                      ┌──────▼──────┐                            │
│                      │   Models    │                            │
│                      │  (Mongoose) │                            │
│                      └──────┬──────┘                            │
└─────────────────────────────┼────────────────────────────────────┘
                              │
                              │ MongoDB Driver
                              │
┌─────────────────────────────▼────────────────────────────────────┐
│                    MongoDB Atlas (Cloud)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │    Users     │  │  Inventory   │  │    Sales     │          │
│  │  Collection  │  │  Collection  │  │  Collection  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│  ┌──────────────┐                                               │
│  │   Invoice    │                                               │
│  │  Sequences   │                                               │
│  └──────────────┘                                               │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. User Login Flow

```
User (Browser)
    │
    │ 1. Enter credentials
    ▼
┌─────────────┐
│  Login Form │
└──────┬──────┘
       │ 2. POST /api/auth/login
       │    { username, password }
       ▼
┌─────────────────┐
│  Auth Routes    │
│  (Backend)      │
└──────┬──────────┘
       │ 3. Verify credentials
       │    Hash password
       ▼
┌─────────────────┐
│  User Model     │
│  (MongoDB)      │
└──────┬──────────┘
       │ 4. User found
       ▼
┌─────────────────┐
│  Generate JWT   │
│  Token          │
└──────┬──────────┘
       │ 5. Return token + user info
       ▼
┌─────────────────┐
│  Frontend       │
│  Store token    │
│  in localStorage│
└─────────────────┘
```

### 2. Create Sale Flow

```
User (Cashier/Admin)
    │
    │ 1. Add items to bill
    │    Apply discount/GST
    │    Enter customer info
    ▼
┌─────────────────┐
│  Billing Page   │
└──────┬──────────┘
       │ 2. POST /api/sales
       │    Authorization: Bearer <token>
       │    { invoice, items, totals }
       ▼
┌─────────────────┐
│  Auth Middleware│
│  Verify JWT     │
└──────┬──────────┘
       │ 3. Token valid
       ▼
┌─────────────────┐
│  Sales Routes   │
└──────┬──────────┘
       │ 4. Validate data
       │    Check stock availability
       ▼
┌─────────────────┐
│  Inventory Model│
│  Update stock   │
│  (Deduct qty)   │
└──────┬──────────┘
       │ 5. Stock updated
       ▼
┌─────────────────┐
│  Sale Model     │
│  Create record  │
└──────┬──────────┘
       │ 6. Sale created
       ▼
┌─────────────────┐
│  Invoice Seq    │
│  Increment      │
└──────┬──────────┘
       │ 7. Return sale record
       ▼
┌─────────────────┐
│  Frontend       │
│  Show success   │
│  Clear bill     │
└─────────────────┘
```

### 3. Inventory Management Flow

```
Admin User
    │
    │ 1. Add/Edit item
    ▼
┌─────────────────┐
│ Inventory Page  │
└──────┬──────────┘
       │ 2. POST/PUT /api/inventory
       │    Authorization: Bearer <token>
       │    { name, type, price, stock }
       ▼
┌─────────────────┐
│  Auth Middleware│
│  Check role     │
└──────┬──────────┘
       │ 3. Verify admin role
       ▼
┌─────────────────┐
│ Inventory Routes│
└──────┬──────────┘
       │ 4. Validate data
       ▼
┌─────────────────┐
│ Inventory Model │
│ Create/Update   │
└──────┬──────────┘
       │ 5. Return updated item
       ▼
┌─────────────────┐
│  Frontend       │
│  Update display │
└─────────────────┘
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Security Layers                      │
└─────────────────────────────────────────────────────────┘

Layer 1: HTTPS/TLS
├─ All communication encrypted
└─ Provided by Vercel

Layer 2: Authentication
├─ JWT tokens (7-day expiry)
├─ Bcrypt password hashing (10 rounds)
└─ Token stored in localStorage

Layer 3: Authorization
├─ Role-based access control
├─ Admin: Full access
└─ Cashier: Limited access (no inventory edit)

Layer 4: Input Validation
├─ Mongoose schema validation
├─ Required field checks
└─ Data type validation

Layer 5: Database Security
├─ MongoDB Atlas encryption at rest
├─ Network IP whitelisting
└─ Database user permissions
```

---

## 📊 Database Relationships

```
┌─────────────┐
│    Users    │
│─────────────│
│ _id         │◄──────┐
│ username    │       │
│ password    │       │
│ displayName │       │
│ role        │       │
└─────────────┘       │
                      │ user (reference)
                      │
┌─────────────┐       │
│ Inventory   │       │
│─────────────│       │
│ _id         │◄──┐   │
│ name        │   │   │
│ type        │   │   │
│ price       │   │   │
│ stock       │   │   │
└─────────────┘   │   │
                  │   │
                  │   │ itemId (reference)
                  │   │
┌─────────────┐   │   │
│   Sales     │   │   │
│─────────────│   │   │
│ _id         │   │   │
│ invoiceNo   │   │   │
│ customer    │   │   │
│ items[]     │───┘   │
│ │ itemId    │       │
│ │ qty       │       │
│ │ rate      │       │
│ totals      │       │
│ user        │───────┘
│ timestamp   │
└─────────────┘

┌─────────────┐
│ Invoice Seq │
│─────────────│
│ year        │
│ sequence    │
└─────────────┘
```

---

## 🚀 Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│                    Developer                             │
└────────────┬─────────────────────────────────────────────┘
             │
             │ git push / vercel deploy
             ▼
┌──────────────────────────────────────────────────────────┐
│                    Vercel Platform                       │
│  ┌────────────────────────────────────────────────────┐  │
│  │              Build Process                         │  │
│  │  1. Install dependencies (npm install)             │  │
│  │  2. Load environment variables                     │  │
│  │  3. Create serverless functions                    │  │
│  └────────────────────────────────────────────────────┘  │
│                          │                               │
│                          ▼                               │
│  ┌────────────────────────────────────────────────────┐  │
│  │         Serverless Functions (Edge Network)        │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐         │  │
│  │  │ /api/auth│  │/api/inv  │  │/api/sales│         │  │
│  │  └──────────┘  └──────────┘  └──────────┘         │  │
│  └────────────────────────────────────────────────────┘  │
│                          │                               │
│                          │ Auto-scaling                  │
│                          │ Global CDN                    │
└──────────────────────────┼───────────────────────────────┘
                           │
                           │ Secure connection
                           ▼
┌──────────────────────────────────────────────────────────┐
│              MongoDB Atlas (Cloud Database)              │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Replica Set (3 nodes for redundancy)              │  │
│  │  - Primary (Read/Write)                            │  │
│  │  - Secondary (Read)                                │  │
│  │  - Secondary (Read)                                │  │
│  └────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Features:                                         │  │
│  │  - Automatic backups                               │  │
│  │  - Point-in-time recovery                          │  │
│  │  - Monitoring & alerts                             │  │
│  │  - Encryption at rest                              │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

---

## 📈 Scalability

```
Current Setup (Free Tier):
├─ Vercel: Unlimited requests, serverless auto-scaling
├─ MongoDB Atlas: 512MB storage, shared cluster
└─ Suitable for: Small to medium business

Growth Path:
├─ Level 1: Current (0-1000 sales/month)
├─ Level 2: MongoDB dedicated cluster (1000-10000 sales/month)
├─ Level 3: Add caching layer (Redis) (10000+ sales/month)
└─ Level 4: Microservices architecture (Enterprise)
```

---

## 🔄 Request/Response Cycle

```
1. User Action (Frontend)
   └─> Click "Add to Inventory"

2. API Call
   └─> POST /api/inventory
       Headers: { Authorization: Bearer <token> }
       Body: { name, type, price, stock }

3. Vercel Serverless Function
   └─> Route to api/index.js
       └─> Express router
           └─> /routes/inventory.js

4. Middleware Chain
   └─> CORS check ✓
       └─> JSON parser ✓
           └─> Auth middleware ✓
               └─> Role check (admin) ✓

5. Business Logic
   └─> Validate input
       └─> Create Mongoose model
           └─> Save to MongoDB

6. Database Operation
   └─> MongoDB Atlas
       └─> Insert document
           └─> Return _id

7. Response
   └─> 201 Created
       └─> { _id, name, type, price, stock }

8. Frontend Update
   └─> Add item to local array
       └─> Re-render table
           └─> Show success notification
```

---

This architecture provides:
- ✅ Scalability (serverless auto-scaling)
- ✅ Security (JWT + HTTPS + encryption)
- ✅ Reliability (MongoDB replica sets)
- ✅ Performance (Edge network + CDN)
- ✅ Maintainability (Clear separation of concerns)
